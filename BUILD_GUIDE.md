# دليل بناء وتحميل تطبيق تذكير شرب الماء

هذا الدليل يشرح جميع الطرق المتاحة لبناء وتحميل ملف APK لتطبيق تذكير شرب الماء.

---

## 📱 الطرق المتاحة

يوجد **ثلاث طرق رئيسية** لبناء وتحميل التطبيق:

1. **باستخدام Android Studio** (الطريقة الموصى بها للمبتدئين)
2. **باستخدام GitHub Actions** (بناء تلقائي في السحابة)
3. **باستخدام سطر الأوامر** (للمطورين المتقدمين)

---

## 🎯 الطريقة الأولى: باستخدام Android Studio

### المتطلبات
- **Android Studio** (آخر إصدار) - [تحميل من هنا](https://developer.android.com/studio)
- **مساحة تخزين:** حوالي 4-5 GB
- **الوقت المطلوب:** 30-45 دقيقة (أول مرة)

### الخطوات التفصيلية

#### 1. تثبيت Android Studio

**للويندوز:**
1. حمّل Android Studio من الرابط أعلاه
2. شغّل ملف التثبيت `.exe`
3. اتبع التعليمات واختر التثبيت الافتراضي
4. انتظر حتى يتم تحميل SDK Components

**للماك:**
1. حمّل ملف `.dmg`
2. اسحب Android Studio إلى مجلد Applications
3. افتح التطبيق واتبع معالج الإعداد

**للينكس:**
```bash
sudo snap install android-studio --classic
```

#### 2. استنساخ المشروع

افتح Terminal (أو Command Prompt في ويندوز) واكتب:

```bash
git clone https://github.com/bbggjah-commits/my-APK-abb.git
```

إذا لم يكن Git مثبتاً:
- **ويندوز:** حمّل من [git-scm.com](https://git-scm.com/)
- **ماك:** `brew install git`
- **لينكس:** `sudo apt install git`

**بديل بدون Git:**
1. اذهب إلى [https://github.com/bbggjah-commits/my-APK-abb](https://github.com/bbggjah-commits/my-APK-abb)
2. اضغط على زر **Code** الأخضر
3. اختر **Download ZIP**
4. فك ضغط الملف

#### 3. فتح المشروع في Android Studio

1. افتح **Android Studio**
2. اختر **File** → **Open**
3. انتقل إلى مجلد `my-APK-abb`
4. اضغط **OK**

#### 4. مزامنة Gradle

عند فتح المشروع لأول مرة:
- سيظهر إشعار **"Gradle Sync"**
- اضغط **Sync Now**
- انتظر حتى تنتهي عملية التحميل (قد تستغرق 5-15 دقيقة)

**إذا ظهرت أخطاء:**
- اضغط **File** → **Invalidate Caches / Restart**
- اختر **Invalidate and Restart**

#### 5. بناء ملف APK

**الطريقة السهلة:**
1. اضغط **Build** من القائمة العلوية
2. اختر **Build Bundle(s) / APK(s)**
3. اختر **Build APK(s)**
4. انتظر حتى تظهر رسالة **"APK(s) generated successfully"**
5. اضغط على **locate** في الإشعار

**موقع الملف:**
```
my-APK-abb/app/build/outputs/apk/debug/app-debug.apk
```

#### 6. تثبيت APK على الهاتف

**الطريقة الأولى: نقل مباشر**
1. وصّل هاتفك بالكمبيوتر عبر USB
2. انسخ ملف `app-debug.apk` إلى هاتفك
3. افتح الملف من مدير الملفات في الهاتف
4. اضغط **Install**
5. قد تحتاج لتفعيل **"Install from unknown sources"**

**الطريقة الثانية: التشغيل المباشر**
1. فعّل **Developer Options** في هاتفك:
   - اذهب إلى **Settings** → **About Phone**
   - اضغط على **Build Number** 7 مرات
2. فعّل **USB Debugging** في Developer Options
3. وصّل الهاتف بالكمبيوتر
4. في Android Studio، اضغط زر **Run** (▶️)
5. اختر هاتفك من القائمة

---

## ☁️ الطريقة الثانية: باستخدام GitHub Actions

هذه الطريقة تبني التطبيق تلقائياً في السحابة **بدون الحاجة لتثبيت أي شيء على جهازك!**

### الخطوات

#### 1. التأكد من تفعيل Actions

1. اذهب إلى [https://github.com/bbggjah-commits/my-APK-abb](https://github.com/bbggjah-commits/my-APK-abb)
2. اضغط على تبويب **Actions**
3. إذا ظهرت رسالة تفعيل، اضغط **"I understand my workflows, go ahead and enable them"**

#### 2. تشغيل Workflow يدوياً

**الطريقة الأولى: عمل Push جديد**
```bash
# استنسخ المشروع
git clone https://github.com/bbggjah-commits/my-APK-abb.git
cd my-APK-abb

# عمل تعديل بسيط
echo "# Build Test" >> README.md

# رفع التعديل
git add .
git commit -m "Trigger build"
git push
```

**الطريقة الثانية: من واجهة GitHub**
1. افتح أي ملف في المستودع
2. اضغط على أيقونة القلم (Edit)
3. أضف سطر فارغ في نهاية الملف
4. اضغط **Commit changes**

#### 3. مراقبة عملية البناء

1. اذهب إلى تبويب **Actions**
2. ستجد workflow قيد التشغيل (دائرة صفراء دوّارة)
3. اضغط عليه لمشاهدة التفاصيل
4. انتظر حتى يتحول إلى علامة ✅ خضراء (يستغرق 5-10 دقائق)

#### 4. تحميل ملف APK

1. بعد انتهاء البناء بنجاح، انزل لأسفل الصفحة
2. ستجد قسم **Artifacts**
3. اضغط على **app-debug** لتحميل ملف ZIP
4. فك ضغط الملف للحصول على `app-debug.apk`

**ملاحظة:** ملفات Artifacts تُحذف تلقائياً بعد 90 يوم.

#### 5. تثبيت APK

- انقل الملف إلى هاتفك وثبّته كما في الطريقة الأولى

---

## 💻 الطريقة الثالثة: باستخدام سطر الأوامر

للمطورين الذين يفضلون Terminal/Command Line.

### المتطلبات
- **JDK 17** أو أحدث
- **Git**

### تثبيت JDK

**للويندوز:**
1. حمّل من [Adoptium](https://adoptium.net/)
2. اختر JDK 17 LTS
3. ثبّت وأضف إلى PATH

**للماك:**
```bash
brew install openjdk@17
```

**للينكس:**
```bash
sudo apt update
sudo apt install openjdk-17-jdk
```

### الخطوات

#### 1. استنساخ المشروع
```bash
git clone https://github.com/bbggjah-commits/my-APK-abb.git
cd my-APK-abb
```

#### 2. منح صلاحيات التنفيذ (لينكس/ماك فقط)
```bash
chmod +x gradlew
```

#### 3. بناء APK

**للينكس/ماك:**
```bash
./gradlew assembleDebug
```

**للويندوز:**
```cmd
gradlew.bat assembleDebug
```

#### 4. العثور على الملف
```bash
# سيكون الملف في:
app/build/outputs/apk/debug/app-debug.apk
```

#### 5. نسخ الملف (اختياري)
```bash
# نسخ إلى سطح المكتب
cp app/build/outputs/apk/debug/app-debug.apk ~/Desktop/
```

---

## 🔧 حل المشاكل الشائعة

### مشكلة: "SDK not found"

**الحل:**
1. افتح Android Studio
2. اذهب إلى **File** → **Project Structure**
3. اختر **SDK Location**
4. تأكد من وجود مسار SDK صحيح

### مشكلة: "Gradle sync failed"

**الحل:**
```bash
# احذف مجلد .gradle
rm -rf .gradle

# أعد المزامنة
./gradlew clean build
```

### مشكلة: "Unable to install APK"

**الحل:**
1. فعّل **"Install from unknown sources"** في إعدادات الهاتف
2. تأكد من أن الملف لم يتلف أثناء النقل
3. جرب إعادة بناء APK

### مشكلة: "Java version mismatch"

**الحل:**
```bash
# تحقق من إصدار Java
java -version

# يجب أن يكون 17 أو أحدث
# إذا كان أقدم، ثبّت JDK 17
```

---

## 📊 مقارنة الطرق

| الطريقة | السهولة | الوقت | المتطلبات | الأفضل لـ |
|---------|---------|-------|-----------|-----------|
| **Android Studio** | ⭐⭐⭐⭐⭐ | 30-45 دقيقة | 4-5 GB | المبتدئين |
| **GitHub Actions** | ⭐⭐⭐⭐ | 5-10 دقائق | حساب GitHub | من لديه إنترنت بطيء |
| **سطر الأوامر** | ⭐⭐ | 10-15 دقيقة | JDK 17 | المطورين المتقدمين |

---

## 🎯 التوصيات

**إذا كنت:**
- 👨‍💻 **مبتدئ:** استخدم Android Studio (الطريقة الأولى)
- ☁️ **لديك إنترنت جيد:** استخدم GitHub Actions (الطريقة الثانية)
- 💻 **مطور متمرس:** استخدم سطر الأوامر (الطريقة الثالثة)

---

## 📱 بعد التثبيت

### استخدام التطبيق

1. افتح التطبيق على هاتفك
2. أدخل الفترة الزمنية بالدقائق (مثلاً: 60)
3. اضغط **"تفعيل التذكيرات"**
4. امنح الأذونات المطلوبة للإشعارات
5. سيبدأ التطبيق بإرسال تذكيرات منتظمة

### الأذونات المطلوبة

عند أول تشغيل، سيطلب التطبيق:
- ✅ **إذن الإشعارات** (Android 13+)
- ✅ **إذن المنبهات الدقيقة**

اضغط **"Allow"** أو **"السماح"** لكل منها.

---

## 🚀 نصائح إضافية

### لتحسين الأداء:
- استخدم **Release Build** بدلاً من Debug:
  ```bash
  ./gradlew assembleRelease
  ```
  
### لتوقيع APK (للنشر):
1. أنشئ keystore:
   ```bash
   keytool -genkey -v -keystore my-release-key.keystore -alias my-key-alias -keyalg RSA -keysize 2048 -validity 10000
   ```
2. أضف معلومات التوقيع في `app/build.gradle`
3. ابنِ Release APK موقّع

### لتصغير حجم APK:
- فعّل ProGuard في `app/build.gradle`:
  ```gradle
  buildTypes {
      release {
          minifyEnabled true
          shrinkResources true
      }
  }
  ```

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع قسم **"حل المشاكل الشائعة"** أعلاه
2. افتح Issue في GitHub: [رابط Issues](https://github.com/bbggjah-commits/my-APK-abb/issues)
3. راجع [توثيق Android الرسمي](https://developer.android.com/studio/build)

---

**تم إعداد هذا الدليل بتاريخ:** نوفمبر 2025  
**الإصدار:** 1.0  
**المشروع:** تطبيق تذكير شرب الماء
